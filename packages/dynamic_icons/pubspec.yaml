name: dynamic_icons
description: "Animated, hover/tap-triggered Flutter icons — stroke-based Lucide glyphs and filled crypto brand marks under one CustomPainter engine."
version: 0.1.0
homepage: https://github.com/dynamicseismic/dynamic-flutter-packages
repository: https://github.com/dynamicseismic/dynamic-flutter-packages/tree/main/packages/dynamic_icons

resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
