name: dynamic_lucide_icons
description: "Beautifully animated, hover/tap-triggered Lucide icons for Flutter — a faithful port of pqoqubbw/icons."
version: 0.1.0
homepage: https://github.com/dynamicseismic/flutter-packages
repository: https://github.com/dynamicseismic/flutter-packages/tree/main/packages/dynamic_lucide_icons

resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
