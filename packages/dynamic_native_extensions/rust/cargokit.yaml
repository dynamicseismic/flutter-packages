# The rust-src component in latest nightly (1.82.0-nightly (94977cb1f 2024-08-06) is broken
# https://github.com/dynamicseismic/flutter-packages/issues/411
#cargo:
# For smaller binaries rebuilt the standard library with panic=abort
#  release:
#    toolchain: nightly
#    extra_flags:
#      - -Z
#      - build-std=panic_abort,std

# Precompiled-binary download is intentionally disabled for this fork
# (no signed prebuilt artifacts are published); the Rust crate builds from source.
